import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  viewStyle: {
    flex: 1,
    height: 50,
    borderRadius: 10,
    backgroundColor: '#893246',
    alignItems: 'center',
    justifyContent: 'center',
  },
  contentContainer: {
    overflow: 'hidden',
    width: '100%',
    margin: 0,
  },
});
